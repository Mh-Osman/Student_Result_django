from django.contrib import admin
from .models import Institution, student, result
# Register your models here.
admin.site.register(Institution)
admin.site.register(student)
admin.site.register(result)
